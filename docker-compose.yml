version: "3.8"

services:
  frontend:
    build: .
    image: arancia-frontend:latest
    restart: always
    env_file: .env
    ports:
      - "${FRONTEND_PORT:-8080}:80"
    networks:
      - dokploy-network

networks:
  dokploy-network:
    external: true
    name: dokploy-network